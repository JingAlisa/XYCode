<!--Components/Msg/List/List.wxml-->

<block wx:for="{{ msgs }}" wx:for-item="msg" wx:key="{{ msg._id }}">
  <msg-item bind:judgmentAdded="refreshTeam" application="{{ msg }}" isCreater="{{ category === 'public' ? true : false }}" />
</block>
